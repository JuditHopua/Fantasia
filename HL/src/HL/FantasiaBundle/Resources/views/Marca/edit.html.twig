{% extends 'FantasiaBundle:Default:base.html.twig' %}

{% block title %} Modificar Marca {% endblock %}

{% block contenido %}
<script>
	function confirmar(){
        if (confirm("¿Realmente desea modificar la marca?") == true) {
			return true
			}
			else{
				return false;
			}     
    }
</script>
	{% include 'FantasiaBundle:Default:menu.html.twig' %}
	
    <h1 id="titulo">Modificar Marca</h1>
	
	{% if mensaje %}
		<div class="form-group" id="addForm">
			<div class="alert alert-danger">{{mensaje}}</div>
		</div>
	{%endif%}
	
	<div class="formAutogenerado">
		{{ form(edit_form) }}
	</div>
	
	<div class="form-group" id="addForm">
		<a href="{{ path('marca') }}" class="btn btn-primary">Volver a la lista</a>
	</div>
{% endblock %}
