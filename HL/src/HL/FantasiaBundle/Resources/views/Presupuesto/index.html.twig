{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Presupuesto list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fecha</th>
                <th>Costoenvio</th>
                <th>Costocolocacion</th>
                <th>Plazoentrega</th>
                <th>Montototalcarpinterias</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('presupuesto_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.costoEnvio }}</td>
                <td>{{ entity.costoColocacion }}</td>
                <td>{{ entity.plazoEntrega }}</td>
                <td>{{ entity.montoTotalCarpinterias }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('presupuesto_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('presupuesto_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('presupuesto_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
